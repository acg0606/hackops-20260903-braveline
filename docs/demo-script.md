# BraveLine two-minute demo script

This script maps only to behavior present in the local build. Do not describe Plus as purchasable until RevenueCat and a store product are configured and verified.

1. **0:00–0:20 — Prepare.** Introduce the synthetic impossible-deadline scenario and the private, device-local rehearsal promise.
2. **0:20–0:50 — Quiet Coach.** Start the guided phrase rail at 15%, show the Courage Line, then let the guide reach 0% before Solo begins.
3. **0:50–1:20 — Solo.** Record a short boundary statement. Mention that microphone permission is requested only here and no audio upload path exists.
4. **1:20–1:45 — Ready and retry.** Play the verified local take. Show that retry asks before replacing the current audio, then reopen only the exact configured clause.
5. **1:45–2:00 — Plus boundary.** Show the honest preview-only screen and name the planned `braveline_pro` entitlement. Do not click or claim a purchase in an unconfigured build.

The local microphone/playback/delete journey is proven in `docs/evidence/native-pass-20260904/`. The clean remediation set in `docs/evidence/native-pass-20260904-r2/` additionally proves safe areas, restart recovery, confirmed replacement, and the exact one-phrase retry. A native 1179×2556 emulator capture is staged at `docs/evidence/submission-assets/braveline-android-1179x2556.png`; before recording the public video, use the final store build and verify a real RevenueCat purchase or restore against that published build.
