# BraveLine proposed two-minute demo script

This is a capture plan, not a recording receipt. The existing 56-second local video is a captioned screenshot montage assembled from retained Android emulator evidence; it is not the continuous recording described below and has no verified public URL. Describe Plus specifically as a RevenueCat Test Store demonstration with no real charge. Custom scenarios, counterpart styles, and rehearsal history are planned features; no trial is available.

1. **0:00–0:20 — Prepare.** Introduce the synthetic impossible-deadline scenario and the private, device-local rehearsal promise.
2. **0:20–0:50 — Quiet Coach.** Start the guided phrase rail at 15%, show the Courage Line, then let the guide reach 0% before Solo begins.
3. **0:50–1:20 — Solo.** Record a short boundary statement. Mention that microphone permission is requested only here and no audio upload path exists.
4. **1:20–1:35 — Ready and retry.** Play the verified local take. Show that retry asks before replacing the current audio, then reopen only the exact configured clause.
5. **1:35–1:55 — RevenueCat Test Store.** Open the published paywall, keep `TEST STORE · NO REAL CHARGE` visible, and show the observed active `braveline_pro` entitlement. Include a successful restore only after a fresh device run verifies it with corrected receipt handling; while that check is pending, do not use the historical UI success message as proof.

The historical local microphone/playback/delete journey is recorded in `docs/evidence/native-pass-20260904/`. The remediation set in `docs/evidence/native-pass-20260904-r2/` additionally records safe areas, restart recovery, confirmed replacement, and the exact one-phrase retry. The observed simulated annual purchase and active entitlement are recorded in `docs/evidence/revenuecat-test-store-20260908/`; successful restore re-verification is pending because the earlier UI could display success after a failed operation. A native 1179×2556 emulator capture is staged at `docs/evidence/submission-assets/braveline-android-1179x2556.png`.
